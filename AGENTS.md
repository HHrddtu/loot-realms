# Loot Realms — Agent Guide

#agents #docs

> [!INFO] Смотри также
> - [[инструкция/инструкция|Правила разработки]] — основной документ с правилами
> - [[инструкция/ORIENTATION|ORIENTATION]] — ориентация по проекту
> - [[README]] — карта содержимого

## Project
**Loot Realms** — браузерная PvE-игра про лут и приключения.

### Технологический стек
- **Phaser 3.80** — игровой движок
- **Vite 8.x** — сборка и dev-сервер
- **Firebase 12.x** — аутентификация, облачные сохранения
- **PeerJS 1.5.x** — P2P мультиплеер
- **Vanilla JavaScript (ES modules)**

### Структура
- `src/main.js` — точка входа
- `src/scenes/` — сцены Phaser
- `src/systems/` — игровые системы
- `src/config/` — конфигурация
- `src/textures/` — ассеты
- `src/*.js` — модули верхнего уровня (auth, save, firebase, multiplayer, talents, bestiary, crafting и т.д.)
- `index.html` — HTML-оболочка и глобальные стили
- `vite.config.js` — Vite (base: './', outDir: 'dist')

### Скрипты
```bash
npm run dev    # запуск dev-сервера
npm run build  # production-сборка в dist/
```

## Команда агентов

| Агент | Роль |
|-------|------|
| `@lead` | Главный архитектор, принимает сложные решения и делегирует |
| `@code` | Написание кода и рефакторинг |
| `@review` | Код-ревью, глубокий анализ, архитектура |
| `@debug` | Быстрая отладка и мелкие фиксы |
| `@ui` | UI/UX, HUD, меню, визуальная полировка |
| `@game-design` | Геймдизайн, баланс, лут, враги, прогрессия |
| `@multiplayer` | PeerJS/Firebase, сеть, синхронизация |
| `@perf` | Оптимизация производительности |
| `@analyst` | Сложная логика, формулы, моделирование систем |
| `@docs` | Документация, комментарии, объяснения |
| `@creative` | Креатив: названия, лор, концепты |
| `@quick` | Быстрые микро-правки |

## Правила работы
- Ведущий агент по умолчанию — `@lead`.
- Для больших задач используй команду `/team <задача>`, чтобы агенты проанализировали задачу вместе.
- Для код-ревью — `/review <фокус>`.
- Для отладки — `/debug <описание бага>`.
- Для дизайна фич — `/design <описание>`.
- Для оптимизации — `/optimize <что оптимизировать>`.
- Всегда запускай `npm run build` после значимых изменений.
- Не ломай форматы сохранений и сетевые протоколы без явного разрешения.
- Не добавляй тяжёлые зависимости без обоснования.
